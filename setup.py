from setuptools import find_packages, setup

setup(
    name="tfo_sensitivity",
    packages=find_packages(),
    version="0.1.0",
    description="Finding out the sensitivity of TFO PPG signals using simulation data in an attempt to estimate the limits of fetal saturation estimation",
    author="Rishad",
    license="MIT",
)
